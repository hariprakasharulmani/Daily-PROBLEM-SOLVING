class Solution {
    public void moveZeroes(int[] nums) {
        
        int j=0;
        int []arr = new int[nums.length];
        for(int i:nums){
            if(i!=0) arr[j++]=i;
        }
        for(int i=0;i<nums.length;i++){
            nums[i] = arr[i];
        }
    }
}